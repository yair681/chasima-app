name: Build Child APK

on:
  push:
    branches: [master]
    paths:
      - 'apps/child/**'
      - '.github/workflows/build-child.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: apps/child/package-lock.json

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install dependencies
        working-directory: apps/child
        run: npm install

      - name: Expo prebuild
        working-directory: apps/child
        run: npx expo prebuild --platform android --clean --no-install
        env:
          EXPO_PUBLIC_API_URL: https://chasima-app.onrender.com

      - name: Make gradlew executable
        working-directory: apps/child/android
        run: chmod +x gradlew

      - name: Build APK
        working-directory: apps/child/android
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: child-app-${{ github.run_number }}
          path: apps/child/android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
